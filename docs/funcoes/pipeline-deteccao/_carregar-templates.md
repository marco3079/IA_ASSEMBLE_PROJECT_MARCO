# `_carregar_templates`

Abre um frame de referência do vídeo B, recorta os templates dos textos das
tarjas Hikvision e os guarda em `_TEMPLATES`. Deve ser chamada antes de
`tarjas_pretas` quando o vídeo usa tarjas móveis.
